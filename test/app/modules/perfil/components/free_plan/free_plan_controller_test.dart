import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ofertas/app/modules/perfil/components/free_plan/free_plan_controller.dart';
import 'package:ofertas/app/modules/perfil/perfil_module.dart';

void main() {
  initModule(PerfilModule());
  // FreePlanController freeplan;
  //
  setUp(() {
    //     freeplan = PerfilModule.to.get<FreePlanController>();
  });

  group('FreePlanController Test', () {
    //   test("First Test", () {
    //     expect(freeplan, isInstanceOf<FreePlanController>());
    //   });

    //   test("Set Value", () {
    //     expect(freeplan.value, equals(0));
    //     freeplan.increment();
    //     expect(freeplan.value, equals(1));
    //   });
  });
}
