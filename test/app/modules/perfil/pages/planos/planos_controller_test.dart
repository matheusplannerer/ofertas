import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ofertas/app/modules/perfil/pages/planos/planos_controller.dart';
import 'package:ofertas/app/modules/perfil/perfil_module.dart';

void main() {
  initModule(PerfilModule());
  // PlanosController planos;
  //
  setUp(() {
    //     planos = PerfilModule.to.get<PlanosController>();
  });

  group('PlanosController Test', () {
    //   test("First Test", () {
    //     expect(planos, isInstanceOf<PlanosController>());
    //   });

    //   test("Set Value", () {
    //     expect(planos.value, equals(0));
    //     planos.increment();
    //     expect(planos.value, equals(1));
    //   });
  });
}
