import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:ofertas/app/modules/perfil/pages/planos/planos_page.dart';

main() {
  testWidgets('PlanosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PlanosPage(title: 'Planos')));
    //  final titleFinder = find.text('Planos');
    //  expect(titleFinder, findsOneWidget);
  });
}
