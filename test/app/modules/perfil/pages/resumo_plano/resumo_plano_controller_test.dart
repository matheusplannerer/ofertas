import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ofertas/app/modules/perfil/pages/resumo_plano/resumo_plano_controller.dart';
import 'package:ofertas/app/modules/perfil/perfil_module.dart';

void main() {
  initModule(PerfilModule());
  // ResumoPlanoController resumoplano;
  //
  setUp(() {
    //     resumoplano = PerfilModule.to.get<ResumoPlanoController>();
  });

  group('ResumoPlanoController Test', () {
    //   test("First Test", () {
    //     expect(resumoplano, isInstanceOf<ResumoPlanoController>());
    //   });

    //   test("Set Value", () {
    //     expect(resumoplano.value, equals(0));
    //     resumoplano.increment();
    //     expect(resumoplano.value, equals(1));
    //   });
  });
}
