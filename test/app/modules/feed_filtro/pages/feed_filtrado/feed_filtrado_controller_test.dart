import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ofertas/app/modules/feed_filtro/pages/feed_filtrado/feed_filtrado_controller.dart';
import 'package:ofertas/app/modules/feed_filtro/feed_filtro_module.dart';

void main() {
  initModule(FeedFiltroModule());
  // FeedFiltradoController feedfiltrado;
  //
  setUp(() {
    //     feedfiltrado = FeedFiltroModule.to.get<FeedFiltradoController>();
  });

  group('FeedFiltradoController Test', () {
    //   test("First Test", () {
    //     expect(feedfiltrado, isInstanceOf<FeedFiltradoController>());
    //   });

    //   test("Set Value", () {
    //     expect(feedfiltrado.value, equals(0));
    //     feedfiltrado.increment();
    //     expect(feedfiltrado.value, equals(1));
    //   });
  });
}
