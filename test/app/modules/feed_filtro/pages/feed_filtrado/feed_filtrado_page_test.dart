import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:ofertas/app/modules/feed_filtro/pages/feed_filtrado/feed_filtrado_page.dart';

main() {
  testWidgets('FeedFiltradoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FeedFiltradoPage(title: 'FeedFiltrado')));
    //  final titleFinder = find.text('FeedFiltrado');
    //  expect(titleFinder, findsOneWidget);
  });
}
