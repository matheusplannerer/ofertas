import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ofertas/app/modules/feed_filtro/feed_filtro_controller.dart';
import 'package:ofertas/app/modules/feed_filtro/feed_filtro_module.dart';

void main() {
  initModule(FeedFiltroModule());
  // FeedFiltroController feedfiltro;
  //
  setUp(() {
    //     feedfiltro = FeedFiltroModule.to.get<FeedFiltroController>();
  });

  group('FeedFiltroController Test', () {
    //   test("First Test", () {
    //     expect(feedfiltro, isInstanceOf<FeedFiltroController>());
    //   });

    //   test("Set Value", () {
    //     expect(feedfiltro.value, equals(0));
    //     feedfiltro.increment();
    //     expect(feedfiltro.value, equals(1));
    //   });
  });
}
