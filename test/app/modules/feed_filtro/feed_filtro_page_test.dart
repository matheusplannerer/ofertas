import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:ofertas/app/modules/feed_filtro/feed_filtro_page.dart';

main() {
  testWidgets('FeedFiltroPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FeedFiltroPage(title: 'FeedFiltro')));
    //  final titleFinder = find.text('FeedFiltro');
    //  expect(titleFinder, findsOneWidget);
  });
}
