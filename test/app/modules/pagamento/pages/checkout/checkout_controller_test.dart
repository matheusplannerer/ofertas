import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ofertas/app/modules/pagamento/pages/checkout/checkout_controller.dart';
import 'package:ofertas/app/modules/pagamento/pagamento_module.dart';

void main() {
  initModule(PagamentoModule());
  // CheckoutController checkout;
  //
  setUp(() {
    //     checkout = PagamentoModule.to.get<CheckoutController>();
  });

  group('CheckoutController Test', () {
    //   test("First Test", () {
    //     expect(checkout, isInstanceOf<CheckoutController>());
    //   });

    //   test("Set Value", () {
    //     expect(checkout.value, equals(0));
    //     checkout.increment();
    //     expect(checkout.value, equals(1));
    //   });
  });
}
