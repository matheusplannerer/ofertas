import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:ofertas/app/modules/login/pages/inserir_codigo/inserir_codigo_page.dart';

main() {
  testWidgets('InserirCodigoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(InserirCodigoPage(title: 'InserirCodigo')));
    //  final titleFinder = find.text('InserirCodigo');
    //  expect(titleFinder, findsOneWidget);
  });
}
